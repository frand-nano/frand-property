import { Button, VerticalBox } from "std-widgets.slint";

export component PayScreen inherits Rectangle {
    callback on-cancel();

    VerticalBox {
        Text {
            text: "Pay Screen";
        }

        Button {
            text: "Cancel";

            clicked => {
                root.on-cancel();
            }
        }
    }
}