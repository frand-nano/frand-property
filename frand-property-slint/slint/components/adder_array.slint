import { Button, VerticalBox, HorizontalBox } from "std-widgets.slint";

export struct AdderArrayData {
    values: [int],
    sum: int,
}

export global AdderArrayDataGlobal {
    in-out property <[AdderArrayData]> data;
}

export component AdderArray inherits Rectangle {
    in property <int> index;

    VerticalBox {
        Text {
            text: "AdderArray[" + index + "]";
        }

        for value[i] in AdderArrayDataGlobal.data[index].values: VerticalBox {
            Text {
                text: "Value[" + i + "]: " + value;
            }

            HorizontalBox {
                Button {
                    text: "+";
                    clicked => {
                        AdderArrayDataGlobal.data[index].values[i] += 1;
                    }
                }

                Button {
                    text: "-";
                    clicked => {
                        AdderArrayDataGlobal.data[index].values[i] -= 1;
                    }
                }
            }
        }

        Text {
            text: "Sum: " + AdderArrayDataGlobal.data[index].sum;
            font-size: 24px;
            horizontal-alignment: center;
        }
    }
}
