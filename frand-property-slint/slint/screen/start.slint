import { Button, VerticalBox } from "std-widgets.slint";
import { Adder } from "../components/adder.slint";
import {
    AdderVec,
    AdderVecDataGlobal,
} from "../components/adder_array.slint";
import { Repeater } from "../components/repeater.slint";

export component StartScreen inherits Rectangle {
    callback on-confirm();

    VerticalBox {
        Text {
            text: "Start Screen";
            font-size: 24px;
            horizontal-alignment: center;
        }

        Repeater { }

        Adder { }

        for item[i] in AdderVecDataGlobal.data: AdderVec {
            global-index: i;
        }

        Button {
            text: "Confirm";
            clicked => {
                root.on-confirm();
            }
        }
    }
}
