export * from "global/index.slint";

import { Screen } from "screen/screen.slint";

export component MainWindow inherits Window {
    title: "Main";
    min-width: 300px;
    min-height: 1300px;

    Screen { }
}
