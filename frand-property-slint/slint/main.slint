import { ScreenGlobalData, ScreenGlobal, Screen } from "screen/screen.slint";
import { AdderGlobalData, AdderGlobal } from "components/adder.slint";
import {
    AdderVecGlobalData,
    AdderVecGlobal,
} from "components/adder_array.slint";
import { RepeaterGlobalData, RepeaterGlobal } from "components/repeater.slint";

export { ScreenGlobalData, ScreenGlobal, AdderGlobalData, AdderGlobal, AdderVecGlobalData, AdderVecGlobal, RepeaterGlobalData, RepeaterGlobal }

export component MainWindow inherits Window {
    title: "Main";
    min-width: 300px;
    min-height: 1300px;

    Screen { }
}
