import { ScreenData, Screen } from "screen/screen.slint";
import { AdderData } from "components/adder.slint";
import { AdderArrayData } from "components/adder_array.slint";

export { ScreenData, AdderData, AdderArrayData }

export component MainWindow inherits Window {
    title: "Main";
    min-width: 300px;
    min-height: 1000px;

    Screen { }
}
